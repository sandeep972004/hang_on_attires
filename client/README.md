# Client — HangOn Attires

React + Vite frontend.

Install & run:

```powershell
cd client
npm install
npm run dev
```

Build for production:

```powershell
npm run build
```

Notes for Vercel:
- Vercel detects Vite projects automatically. Set the build command to `npm run build` and the output directory to `dist`.
